result
result-*
.direnv/

# Machine-specific / installer-generated (regenerated by install.sh from the .in).
# Ship the template, not the version with a hardcoded $HOME baked in.
dotfiles/matugen/config.toml
